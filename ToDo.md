# 🏥 خطة تنفيذ مشروع عيادة السكري بالذكاء الاصطناعي
# 📋 Diabetes Clinic AI — DeepSeek Execution Plan
### 📋 الأمر الاول (Prompt 1) لـ DeepSeek:

```
أنت مهندس بيانات (Data Engineer). اكتب لي سكربت Python محلي يستخدم مكتبات مثل
Pandas و SQLAlchemy للاتصال بقاعدة بيانات MySQL الخاصة بالعيادة.

السكربت يجب أن يقوم بالآتي:
1. جلب بيانات المرضى (القياسات الحيوية، الفحوصات، درجات واغنر، الأدوية، ونسب التحسن)
2. معالجة البيانات النصية وتحويلها إلى أرقام (One-Hot Encoding):
   - مثل تحويل 'النبض: معدوم' أو 'الإحساس: متناقض' إلى قيم رياضية
3. تنظيف البيانات والتعامل مع القيم المفقودة (Missing Values)
4. حفظ البيانات المعالجة بصيغة CSV جاهزة لإدخالها في نماذج تعلم الآلة

الهيكل المطلوب:
├── data_pipeline/
│   ├── __init__.py
│   ├── config.py          # إعدادات الاتصال بقاعدة البيانات
│   ├── extractor.py       # استخراج البيانات من MySQL
│   ├── transformer.py     # معالجة وتحويل البيانات
│   ├── cleaner.py         # تنظيف البيانات
│   └── main.py            # تشغيل الـ Pipeline بالكامل

أريد الكود مع تعليقات عربية واضحة وشاملة.
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## المرحلة الثانية: خوارزميات التوقع والتحليل (ML)
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 📌 الهدف:
بناء نظام ذكاء اصطناعي مدمج (Ensemble Model) للتنبؤ بالمخاطر والشفاء.

### 📋 الأمر الثاني (Prompt 2) لـ DeepSeek:

```
أنت عالم بيانات (Data Scientist) خبير في المجال الطبي. بناءً على البيانات المعالجة
في الخطوة السابقة، اكتب لي سكربت Python يبني نظام ذكاء اصطناعي مدمج (Ensemble Model)
باستخدام Random Forest و XGBoost.

الخوارزمية يجب أن تقوم بثلاث مهام رئيسية:

### المهمة 1: التنبؤ بالخطر (Risk Prediction)
- حساب نسبة مئوية دقيقة لخطر 'تدهور القرحة' أو 'الحاجة إلى البتر'
- المدخلات: الإحساس، النبض، التراكمي، التدخين، عمق الجرح
- المخرج: نسبة مئوية للخطر (0-100%)

### المهمة 2: توقع مدة الشفاء (Healing Time Estimation)
- خوارزمية انحدار (Regression) تتوقع عدد الأسابيع اللازمة للشفاء
- المدخلات: المضاد الحيوي المستخدم، مستوى السكر
- المخرج: عدد أسابيع متوقع + مستوى الثقة

### المهمة 3: توليد خطة العلاج والتغذية (Rule-Based AI)
- نظام توصيات يقترح:
  - النظام الغذائي المناسب
  - تعليمات العناية المنزلية
- القواعد:
  - نظام قليل الكربوهيدرات إذا كان التراكمي مرتفعاً
  - توصيات تفحص القدم اليومي إذا كان الإحساس معدوماً
  - تعليمات خاصة حسب حالة المريض

الهيكل المطلوب:
├── ai_engine/
│   ├── __init__.py
│   ├── risk_predictor.py      # نموذج التنبؤ بالخطر
│   ├── healing_estimator.py   # نموذج تقدير الشفاء
│   ├── treatment_recommender.py  # نظام التوصيات
│   ├── ensemble_model.py      # النموذج المدمج
│   ├── trainer.py             # تدريب النماذج
│   └── main.py                # الواجهة الرئيسية

أريد الكود جاهزاً لتدريب النموذج، مع حفظه باستخدام joblib لاستخدامه لاحقاً.
أضف دقة النموذج (accuracy) وأي مقياس أداء مناسب.
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## المرحلة الثالثة : الربط بين PHP و Python (Integration)
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 📌 الهدف:
جعل واجهة PHP تتصل مع محرك Python لعرض النتائج والتوقعات لحظياً.

### 📋 الأمر الثالث (Prompt 3) لـ DeepSeek:

```
أنت مهندس برمجيات متكامل (Full-Stack). لدي:
- واجهة أمامية وقاعدة بيانات بـ PHP
- نموذج ذكاء اصطناعي محلي بـ Python

أريد كوداً لربط النظامين محلياً بأفضل وأسرع طريقة.

المتطلبات:
1. أنشئ Flask API في Python على localhost:5000:
   - POST /api/predict/risk     → توقع الخطر
   - POST /api/predict/healing  → توقع مدة الشفاء
   - POST /api/recommend        → توصيات العلاج والتغذية
   - GET  /api/health           → فحص حالة الخادم
   - كل الاستجابات بصيغة JSON

2. أنشئ ملف PHP includes/ai_client.php:
   - دالة callAI($endpoint, $data) ترسل طلب HTTP إلى Flask API
   - معالجة أخطاء الاتصال
   - timeout مناسب (5 ثوانٍ)

3. أنشئ واجهة Dashboard في modules/ai/:
   - صفحة تحليل مريض معين (تحتوي على زر "تحليل بالذكاء الاصطناعي")
   - عند الضغط: ترسل بيانات المريض إلى Python API
   - تعرض النتائج بشكل بصري:
     * شريط تقدم للخطر (أخضر ← أحمر)
     * عدد أسابيع الشفاء المتوقع
     * قائمة التوصيات العلاجية والغذائية

4. أضف نظام تنبيهات:
   - إذا كان الخطر > 80% → تنبيه أحمر "خطر عالي - يُنصح بالمراجعة العاجلة"
   - إذا كان الخطر > 50% → تنبيه أصفر "خطر متوسط - مراقبة مستمرة"
   - إذا كان الخطر < 50% → تنبيه أخضر "خطر منخفض"

الهيكل المطلوب:
├── ai_api/
│   ├── app.py              # Flask API Server
│   ├── requirements.txt    # المتطلبات
│   └── models/             # النماذج المحفوظة (.joblib)
├── www/
│   └── includes/
│       └── ai_client.php   # عميل الاتصال بـ Python
├── www/
│   └── modules/
│       └── ai/
│           ├── dashboard.php   # لوحة تحليل الذكاء الاصطناعي
│           └── analyze.php     # صفحة تحليل مريض معين

أريد تشغيل النظام كاملاً بخطوتين:
1. تشغيل Python: python ai_api/app.py
2. تشغيل PHP: من أي سيرفر محلي (XAMPP/WAMP)
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## أوامر التثبيت والتشغيل
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### تثبيت المتطلبات البرمجية:

```bash
# Python - المتطلبات
pip install pandas sqlalchemy pymysql scikit-learn xgboost flask joblib

# PHP - لا يحتاج متطلبات إضافية (MySQLi مدمج في PHP)
```

### تشغيل النظام:

```bash
# 1. تشغيل محرك الذكاء الاصطناعي
cd ai_api
python app.py

# 2. تشغيل سيرفر PHP (من XAMPP أو)
cd www
php -S localhost:8080

# 3. فتح المتصفح
# http://localhost:8080
```

---
